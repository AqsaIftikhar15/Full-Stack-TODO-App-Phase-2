{{- if .Values.auditService }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-app-event-driven.fullname" . }}-audit-service
  labels:
    {{- include "todo-app-event-driven.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "todo-app-event-driven.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: audit-service
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
        {{- if .Values.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: "audit-service"
        dapr.io/app-port: "{{ .Values.auditService.service.port }}"
        dapr.io/config: "{{ .Values.dapr.config }}"
        {{- end }}
      {{- end }}
      labels:
        {{- include "todo-app-event-driven.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: audit-service
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "todo-app-event-driven.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-audit-service
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.auditService.image.repository }}:{{ .Values.auditService.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.auditService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.auditService.service.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.auditService.resources | nindent 12 }}
          env:
            - name: DAPR_HTTP_PORT
              value: "3500"
            - name: DATABASE_URL
              value: {{ .Values.backend.database.url | quote }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 8 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 8 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 8 }}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app-event-driven.fullname" . }}-audit-service
  labels:
    {{- include "todo-app-event-driven.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.auditService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-app-event-driven.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: audit-service
{{- end }}